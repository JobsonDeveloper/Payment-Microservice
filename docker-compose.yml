services:
  payment-microservice:
    build: .
    ports:
      - "8085:8085"
    networks:
      - ecommerce-network
networks:
  ecommerce-network:
    name: ecommerce-network
    driver: bridge
    external: true